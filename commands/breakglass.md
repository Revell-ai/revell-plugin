---
description: "For humans: emergency recovery menu — refresh payload, inspect buffers, get the restart command, or call for help."
disable-model-invocation: true
user-invocable: true
---

Call revell_skill({ name: "breakglass" }) and follow what it returns.

If it does not answer, say so plainly rather than improvising a recovery
menu — the whole value of this command is that the steps are the real ones.
