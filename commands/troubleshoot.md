---
description: "For humans: work through a Revell problem step by step — what is failing, what to try, and what to send if it still will not go."
disable-model-invocation: true
user-invocable: true
---

Call revell_skill({ name: "troubleshoot" }) and follow what it returns.

If it does not answer, say so plainly rather than inventing steps — a made-up
recovery is worse than none when someone is already stuck.
