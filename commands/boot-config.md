---
description: "View or change how much of your agent's memory Revell keeps loaded — recent history, boot budget, and how much detail per memory. Same knobs as the dashboard, from inside the session."
disable-model-invocation: true
user-invocable: true
---

Call revell_skill({ name: "boot-config" }) and follow what it returns.
