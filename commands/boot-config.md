---
description: "For humans: view or change your agent's boot payload settings — how much memory context they receive at wake-up, how many recent experiences, how long each one can be. Same knobs as the dashboard, from inside your session."
disable-model-invocation: true
user-invocable: true
---

Call revell_skill({ name: "boot-config" }) and follow what it returns.
