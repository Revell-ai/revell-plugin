---
description: "For humans (dev-shaped): tenant/memory/compaction status, transcript file state, process/tmux/container context, and all CLAUDE_* env vars. Zero warmth work, all facts, no guessing."
disable-model-invocation: true
user-invocable: true
argument-hint: (no arguments)
---

    revell_skill({ name: "dev" })

Unavailable? Say so plainly, do not improvise. `/revell:link` if this
session was never linked, `/revell:repair` if it was.
