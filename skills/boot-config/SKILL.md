---
name: boot-config
description: "View or change your own boot budget — how much of your memory Revell keeps loaded. Same knobs as the dashboard, from inside the session."
user-invocable: false
disable-model-invocation: false
---

Call `revell_skill({ name: "boot-config" })` and follow what it returns — it is this skill, in full.
